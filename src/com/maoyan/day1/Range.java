package com.maoyan.day1;

/**
 * Created by jiangdongyu on 2017/4/18.
 */
public class Range {
    /*
    List scores;
    Iterable belowMedian =Iterables.filter(scores,Range.lessThan(median));
    Range validGrades = Range.closed(1, 12);
    for(int grade : ContiguousSet.create(validGrades, DiscreteDomain.integers())) {

    }

    区间实例由Range静态方法获取
(a..b)	open(C, C)
[a..b]	closed(C, C)
[a..b)	closedOpen(C, C)
(a..b]	openClosed(C, C)
(a..+∞)	greaterThan(C)
[a..+∞)	atLeast(C)
(-∞..b)	lessThan(C)
(-∞..b]	atMost(C)
(-∞..+∞)	all()

区间运算
Range.closed(1, 3).contains(2);//return true 判断是否包含某个值

查询运算
hasLowerBound()和hasUpperBound()：判断区间是否有特定边界，或是无限的；
lowerBoundType()和upperBoundType()：返回区间边界类型，CLOSED或OPEN；如果区间没有对应的边界，抛出IllegalStateException；
lowerEndpoint()和upperEndpoint()：返回区间的端点值；如果区间没有对应的边界，抛出IllegalStateException；
isEmpty()：判断是否为空区间。

包含[enclose]
区间之间的最基本关系就是包含[encloses(Range)]


相连[isConnected]
Range.isConnected(Range)判断区间是否是相连的。

交集[intersection]
Range.intersection(Range)返回两个区间的交集

跨区间[span]
Range.span(Range)返回”同时包括两个区间的最小区间”，如果两个区间相连，那就是它们的并集。

    */

}
